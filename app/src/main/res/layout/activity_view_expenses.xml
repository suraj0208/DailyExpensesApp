<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.suraj.dailyexpenses.ViewExpensesActivity">

    <Spinner
        android:id="@+id/spinDates"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingEnd="@dimen/activity_vertical_margin"
        android:paddingLeft="@dimen/activity_vertical_margin"
        android:paddingRight="@dimen/activity_vertical_margin"
        android:paddingStart="@dimen/activity_vertical_margin" />

    <TextView
        android:id="@+id/tvExpenditureForDate"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/spinDates"
        android:layout_margin="10dp"
        android:text="@string/app_name" />

    <TextView
        android:id="@+id/tvExpenditureForCurrentMonth"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/tvExpenditureForDate"
        android:layout_margin="10dp"
        android:text="@string/app_name" />

    <Button
        android:id="@+id/btnViewForMonth"
        android:layout_width="25dp"
        android:layout_height="25dp"
        android:layout_alignParentEnd="true"
        android:layout_alignParentRight="true"
        android:layout_alignTop="@id/tvExpenditureForCurrentMonth"
        android:layout_marginRight="10dp"
        android:background="@drawable/ic_calendar" />

    <ListView
        android:id="@+id/lstViewItems"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/tvExpenditureForCurrentMonth"
        android:layout_marginTop="10dp">

    </ListView>
</RelativeLayout>
